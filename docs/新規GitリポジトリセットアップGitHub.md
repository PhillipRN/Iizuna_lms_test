# 新規 Git リポジトリセットアップガイド

## 🎯 目的

新しく作成した GitHub リポジトリ（https://github.com/PhillipRN/Iizuna_lms_test.git）に、ローカル環境と AWS 複製環境を接続します。

---

## 📋 開発フロー

```
┌─────────────────┐
│ ローカル Mac     │  ← VSCode で開発
│ (開発環境)       │
└────────┬────────┘
         │ git push
         ↓
┌─────────────────┐
│ GitHub          │  ← コード管理
│ Iizuna_lms_test │
└────────┬────────┘
         │ git pull
         ↓
┌─────────────────┐
│ AWS EC2         │  ← 動作確認
│ (56.155.92.207) │
└─────────────────┘
```

---

## パート 1: ローカル環境の Git 設定（10 分）

### ステップ 1-1: 現在の Git 状態を確認

```bash
# ローカルマシンで実行
cd /Users/phillipr.n./Documents/KUTO/いいずな/iizuna_apps_dev/iizuna-lms-main

# 現在の Git 状態を確認
git status

# リモートリポジトリを確認
git remote -v

# 期待される出力: 既存のリモートが表示される
# または何も表示されない（リモートが設定されていない場合）
```

### ステップ 1-2: 既存のリモートを削除（既存がある場合のみ）

```bash
# 既存のリモート一覧を確認
git remote -v

# リモートがある場合は削除
git remote remove origin

# 確認（何も表示されなければOK）
git remote -v
```

### ステップ 1-3: 新しいリモートを追加

```bash
# 新しいリポジトリを origin として追加
git remote add origin https://github.com/PhillipRN/Iizuna_lms_test.git

# 確認
git remote -v

# 期待される出力:
# origin  https://github.com/PhillipRN/Iizuna_lms_test.git (fetch)
# origin  https://github.com/PhillipRN/Iizuna_lms_test.git (push)
```

### ステップ 1-4: ブランチ名を確認・変更

```bash
# 現在のブランチを確認
git branch

# main ブランチでない場合、リネーム
git branch -M main

# 確認
git branch
# * main
```

### ステップ 1-5: .gitignore の確認（重要！）

機密情報がコミットされないように確認：

```bash
# .gitignore の内容を確認
cat .gitignore

# 必須項目が含まれているか確認:
# ✅ app/config.ini
# ✅ app/token.ini
# ✅ app/firebase_auth.json
# ✅ vendor/
# ✅ .env
```

**もし含まれていない場合は追加**：

```bash
# 不足している項目を追加
cat >> .gitignore << 'EOF'

# 機密情報（絶対にコミットしない）
app/config.ini
app/token.ini
app/firebase_auth.json
*.pem
*.key
.env
.env.local

# 依存関係
vendor/

# キャッシュ
app/smarty_template_c/
app/Temps/
EOF

# 確認
cat .gitignore
```

### ステップ 1-6: 環境情報ファイルを作成

```bash
# リポジトリの説明を作成
cat > REPOSITORY_INFO.md << 'EOF'
# いいずな LMS - 開発・検証用リポジトリ

## このリポジトリについて

このリポジトリは、いいずな LMS の開発・検証用の複製環境です。

### 環境情報

- **ローカル開発環境**: macOS
- **AWS 複製環境**: EC2 (56.155.92.207)
- **目的**: 機能追加・検証・学習

### ブランチ戦略

- `main`: 安定版（AWS環境と同期）
- `develop`: 開発中の機能
- `feature/*`: 機能追加ブランチ

### 重要な注意事項

⚠️ **このリポジトリは本番環境とは無関係です**

- 本番環境へのデプロイは行わない
- 既存の開発環境（spapp-dev-ec2）とは独立
- 学習・検証目的での使用

### 開発フロー

1. ローカルで開発（VSCode）
2. Git にコミット・プッシュ
3. AWS 環境で git pull
4. ブラウザで動作確認

### 担当者

- PhillipRN

### 作成日

2025-01-03
EOF

# ファイルを確認
cat REPOSITORY_INFO.md
```

### ステップ 1-7: README を更新

```bash
# 既存の ReadMe.md をバックアップ
cp ReadMe.md ReadMe.original.md

# 新しい README を作成
cat > README.md << 'EOF'
# いいずな LMS - 開発・検証環境

学校用 LMS（Learning Management System）の開発・検証用リポジトリです。

## 🚀 環境

### ローカル開発環境
- **OS**: macOS
- **開発ツール**: VSCode, Docker Desktop
- **ローカル URL**: http://localhost:8080

### AWS 複製環境
- **インスタンス**: spapp-dev-clone-20250103-prn
- **IP アドレス**: 56.155.92.207
- **URL**: http://56.155.92.207

## 📋 セットアップ

### ローカル Docker 環境

```bash
# 初回セットアップ
make setup

# 起動
make up

# 停止
make down
```

詳細: [QUICKSTART.md](QUICKSTART.md)

### AWS 環境

詳細: [docs/AWS複製環境セットアップ手順.md](docs/AWS複製環境セットアップ手順.md)

## 🔧 開発フロー

1. ローカルで開発
2. Git にコミット・プッシュ
3. AWS 環境で最新コードを取得
4. ブラウザで動作確認

```bash
# ローカルで開発
git add .
git commit -m "feat: 新機能追加"
git push origin main

# AWS 環境で更新
ssh spapp-dev-clone
cd /var/www/iizuna_lms/
git pull origin main
```

## 📚 ドキュメント

- [クイックスタート](QUICKSTART.md)
- [AWS 環境セットアップ](docs/AWS複製環境セットアップ手順.md)
- [SSH 設定ガイド](docs/SSH設定ガイド.md)
- [開発環境構築ガイド](docs/開発環境構築ガイド.md)

## ⚠️ 注意事項

- **本番環境とは無関係**: このリポジトリは学習・検証用です
- **機密情報の管理**: `app/config.ini` など機密情報は `.gitignore` に含まれています
- **既存環境への影響なし**: 既存の開発環境（spapp-dev-ec2）とは独立しています

## 📞 問い合わせ

PhillipRN
EOF

# 確認
cat README.md
```

### ステップ 1-8: Git にコミット

```bash
# 変更されたファイルを確認
git status

# すべてステージング
git add .

# コミット
git commit -m "初期コミット: 新しいGitリポジトリとして設定"

# コミット確認
git log -1
```

### ステップ 1-9: GitHub に初回プッシュ

```bash
# GitHub にプッシュ
git push -u origin main

# 成功すると、GitHub にコードがアップロードされます
```

**GitHub の認証が求められた場合**：

```bash
# Personal Access Token (PAT) を使用
# Username: PhillipRN
# Password: <GitHub PAT>

# または SSH 認証に変更
git remote set-url origin git@github.com:PhillipRN/Iizuna_lms_test.git
git push -u origin main
```

### ステップ 1-10: GitHub で確認

ブラウザで https://github.com/PhillipRN/Iizuna_lms_test を開いて確認：

- ✅ ファイルがアップロードされている
- ✅ README.md が表示されている
- ✅ docs/ ディレクトリが存在する

---

## パート 2: AWS 環境の Git 設定（10 分）

### ステップ 2-1: AWS 環境に接続

```bash
# ローカルマシンで SSH 接続
ssh spapp-dev-clone
```

### ステップ 2-2: アプリケーションディレクトリに移動

```bash
# アプリケーションディレクトリへ
cd /var/www/iizuna_lms/

# 現在の Git 状態を確認
git status
```

### ステップ 2-3: 既存のリモートを削除

```bash
# 現在のリモートを確認
git remote -v

# 既存のリモートを upstream にリネーム（参照用に保持）
git remote rename origin upstream

# 確認
git remote -v
# upstream が表示される
```

### ステップ 2-4: 新しいリモートを追加

```bash
# 新しいリポジトリを origin として追加
git remote add origin https://github.com/PhillipRN/Iizuna_lms_test.git

# 確認
git remote -v

# 期待される出力:
# origin    https://github.com/PhillipRN/Iizuna_lms_test.git (fetch)
# origin    https://github.com/PhillipRN/Iizuna_lms_test.git (push)
# upstream  git@github.com:organization/original-repo.git (fetch)
# upstream  git@github.com:organization/original-repo.git (push)
```

### ステップ 2-5: 最新コードを取得

```bash
# GitHub から最新のコードを取得
git fetch origin

# main ブランチに切り替え
git checkout main

# または、既存のブランチから強制的に上書き
git reset --hard origin/main

# 確認
git log -1
```

### ステップ 2-6: 設定ファイルの調整

```bash
# 設定ファイルを確認（git管理外なので残っているはず）
ls -la app/config.ini

# なければサンプルからコピー
if [ ! -f app/config.ini ]; then
    cp app/config.ini.example app/config.ini
fi

# 設定ファイル編集
nano app/config.ini
```

**設定する項目**：

```ini
DEBUG_MODE = 1
DISPLAY_ERROR_ALL = 1
WWW_ROOT_URL = http://56.155.92.207

DB_HOST = db-dev.spapp-db.localdomain
DB_NAME = iizunaLMS
DB_USER = iizunaLMS
DB_PASS = Gawbvgt2f983mru

# 以下省略...
```

### ステップ 2-7: 動作確認

```bash
# Apache 起動確認
sudo systemctl status httpd

# 起動していなければ起動
sudo systemctl start httpd

# ログ確認
sudo tail -20 /var/log/httpd/error_log
```

---

## パート 3: 開発フローの確立（理解）

### 日常的な開発フロー

#### 1. ローカルで開発

```bash
# ローカルマシンで
cd /Users/phillipr.n./Documents/KUTO/いいずな/iizuna_apps_dev/iizuna-lms-main

# VSCode で開く
code .

# ファイルを編集（例: app/Controllers/StudentController.php）
# ...編集...

# 変更を確認
git status

# ステージング
git add app/Controllers/StudentController.php

# コミット
git commit -m "feat: 生徒一覧の表示機能を追加"

# GitHub にプッシュ
git push origin main
```

#### 2. AWS 環境で最新コードを取得

```bash
# AWS 環境に接続
ssh spapp-dev-clone

# アプリケーションディレクトリへ
cd /var/www/iizuna_lms/

# 最新コードを取得
git pull origin main

# エラーがないか確認
sudo tail -20 /var/log/httpd/error_log
```

#### 3. ブラウザで動作確認

```
http://56.155.92.207
```

#### 4. 問題があれば修正

```bash
# ローカルで修正
# → コミット
# → プッシュ
# → AWS で git pull
# → 再確認
```

---

## 🔀 ブランチを使った開発（応用）

### 機能追加時のフロー

```bash
# ローカルで新しいブランチを作成
git checkout -b feature/new-function

# 開発
# ...

# コミット
git add .
git commit -m "feat: 新機能の追加"

# GitHub にプッシュ
git push origin feature/new-function

# AWS 環境でテスト
ssh spapp-dev-clone
cd /var/www/iizuna_lms/
git fetch origin
git checkout feature/new-function
git pull origin feature/new-function

# テストOKなら main にマージ
git checkout main
git merge feature/new-function
git push origin main
```

---

## 🛡️ セキュリティチェックリスト

リポジトリを公開する前に確認：

- [ ] `app/config.ini` が `.gitignore` に含まれている
- [ ] `app/token.ini` が `.gitignore` に含まれている
- [ ] `app/firebase_auth.json` が `.gitignore` に含まれている
- [ ] `*.pem` (SSH キー) が `.gitignore` に含まれている
- [ ] データベースパスワードが平文でコミットされていない
- [ ] API キーが平文でコミットされていない

### もしコミットしてしまった場合

```bash
# 機密情報を含むファイルを履歴から削除
git rm --cached app/config.ini
git commit -m "chore: config.iniをgit管理から除外"
git push origin main

# GitHub でリポジトリを Private に変更することを推奨
```

---

## 💡 便利な Git コマンド

### よく使うコマンド

```bash
# 状態確認
git status

# 変更の差分を確認
git diff

# コミット履歴
git log --oneline --graph

# 特定のファイルの履歴
git log -p app/config.ini.example

# リモートの最新を確認（マージしない）
git fetch origin

# ブランチ一覧
git branch -a

# 未追跡ファイルを削除
git clean -fd
```

### トラブルシューティング

```bash
# ローカルの変更を破棄して最新に戻す
git reset --hard origin/main

# 特定のファイルを最新に戻す
git checkout origin/main -- app/Controllers/SomeController.php

# マージを取り消す
git merge --abort

# コミットを取り消す（直前のコミットのみ）
git reset --soft HEAD^
```

---

## ✅ 完了チェックリスト

### ローカル環境

- [ ] リモートリポジトリを新しいものに変更した
- [ ] `.gitignore` を確認した
- [ ] REPOSITORY_INFO.md を作成した
- [ ] README.md を更新した
- [ ] GitHub に初回プッシュした
- [ ] GitHub でファイルを確認した

### AWS 環境

- [ ] リモートリポジトリを変更した
- [ ] 最新コードを取得した
- [ ] 設定ファイル（config.ini）を編集した
- [ ] Apache が起動している
- [ ] ブラウザでアクセスできる

---

## 🎯 次のステップ

Git 設定が完了したら：

1. **ローカル Docker 環境で開発**
   ```bash
   cd /Users/phillipr.n./Documents/KUTO/いいずな/iizuna_apps_dev/iizuna-lms-main
   make setup
   ```

2. **小さな変更でテスト**
   - CSS の色を変える
   - テキストを変更
   - Git にコミット・プッシュ

3. **AWS 環境で確認**
   ```bash
   ssh spapp-dev-clone
   cd /var/www/iizuna_lms/
   git pull origin main
   ```

4. **開発フローに慣れる**

---

## 📚 関連ドキュメント

- [QUICKSTART.md](../QUICKSTART.md) - ローカル Docker 環境
- [AWS複製環境セットアップ手順.md](AWS複製環境セットアップ手順.md) - AWS 環境設定
- [SSH設定ガイド.md](SSH設定ガイド.md) - SSH 接続設定
- [開発環境構築ガイド.md](開発環境構築ガイド.md) - 詳細ガイド

---

**これで、安全に開発できる環境が整いました！** 🎉

